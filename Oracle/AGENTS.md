# Oracle lane isolation

Read the root working rules. This context translates original Prolog against
`human/DECISIONS.md` and `Interface/`; it must not inspect `harness/`, `gen/`,
their tests, or their implementation reports. The orchestrator may invoke a
harness CLI and pass retained inputs, outputs, diagnostics and verbatim meter
mismatches, never serializer implementation details.

Use a fresh context per section, in order 7703, 3306, 3301, 2, 63, 68, 151,
152, 1. R5 work is authorized on installed Option A's narrowed domain; its
source-mode, eligibility-decider, finite-universe, decrease, phase and fuel
proof obligations remain open. An open proof is not a completed definition or
a reference value. No exhaustion-as-answer, semantic repair or filtered input.

Every entry wrapper must require the shared actual-tuple query admission and
every R5/R8 invocation its actual-time certificate. Valid/ValidStip alone do
not check a query. Do not replace an unbound date with a default, use the outer
year for a supplied Workday, or reapply V8 at a derived Workday year.

Shared schemas/types belong in Interface, not private parallel definitions.
Request changes through the orchestrator before changing that shared boundary.
Do not choose new modes or projections. Cite every translated source clause and
its NAF/CUT/AGG rules. No partial, sorry, native_decide, unproved axioms or
test-only production guard instances. Open dependencies stay explicit and are
not advertised as complete target implementations.

Report a missing semantic decision/design flaw before revising a design. End
each handoff with Prolog assumptions and the circuit breaker: three failed edit
cycles on one test means stop and report.
