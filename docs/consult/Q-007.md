# Q-007: installing Dev's accepted policies; acceptance-tool findings

## Phase

Checkpoint 0, before the independent owner meter is installed.

## Lane

infra

## Question

Dev explicitly accepted P-SUMLIST, P-RUNTIME and five parity policies in the
attached decision text, staged verbatim at
`docs/contracts/OWNER_DECISIONS_2026-09-21.txt`. That latest owner direction
supersedes conflicting historical statements in your initial prompt. Do not
reopen those accepted decisions. The builder is installing them in PROTOCOL,
RUNTIME and PARITY, preserving PLAN and all of human/.

Please append the owner's acceptance to docs/DECISION_LOG.md, synchronize its
status index, and clearly mark the earlier P-SUMLIST and P-RUNTIME proposal
bodies as historical/superseded without changing their bodies. The runtime
acceptance is ONE entry affecting BOTH PROTOCOL B001 and RUNTIME.md; the status
index must no longer say the old runtime contract remains operative. Preserve
all previous entry bodies and all A files. Acceptance is not evidence that
vendoring, GHCR publication, archival deposit or native CI has happened.

Two builder acceptance issues need classification before any meter verdict:

1. `scripts/parity_conformance.py` makes `meter = Path(arguments.meter)` and
   passes `str(meter)` to Python after setting cwd to a per-fixture temporary
   directory. Dev's specified relative-path invocation therefore cannot open
   the file. Exit-2 fixtures see nonempty stderr and are reported `ok` for
   Python's file-not-found error, not for rejection by the meter. Does fixing
   invocation to resolve the path before changing cwd and explicitly reporting
   invocation errors count as restoration of the existing CLI contract? Dev
   allowed only the invalid-ID fixture promotion and said not to adjust the
   suite to reconcile a meter divergence. No owner meter has been run or read.
   We have paused this acceptance lane, not altered expected outcomes.
2. Dev says Checkpoint 0 requires verify_phase0.sh to report zero failures and
   zero outstanding plus explicit signoff. That script currently counts absent
   Checkpoint 2 exploits and Checkpoint 3a statements as outstanding. Prior
   owner instructions explicitly say those are later checkpoint artifacts.
   May the builder report these as later-phase obligations separately from
   Checkpoint 0, or does changing this gate's accounting require explicit Dev
   approval? No changes have been made to that script.

Also flag (do not resolve by inventing policy) any conflict in the verbatim
five policies that would prevent mechanically faithful installation. The
owner specifically places dot-prefixed directories in policy 1, prior to
policy 2; do not silently drop that qualification.

## What was tried

- Read the required plan, protocol, handoff, state, parity/runtime contracts,
  the suite and the latest owner decision. The owner meter is absent; only its
  existence was checked.
- Ran the staged exit-2 stub through the specified relative-path command.
  The suite reported 7/20 checks passing despite no stub execution.
- Traced one `empty-input-population` call through the suite's own `run_case`
  in an isolated temporary fixture. Python reported it could not open
  `<temporary>/empty-input-population/run/docs/contracts/parity/check.py`;
  the suite returned `(True, '')`.
- Existing `verify_phase0.sh` explicitly lists exploit files as Checkpoint 2
  and statements as Checkpoint 3a, then includes both in Checkpoint 0's
  outstanding count.

## Files involved

- docs/contracts/OWNER_DECISIONS_2026-09-21.txt
- docs/DECISION_LOG.md
- docs/PROTOCOL.md
- docs/contracts/PARITY.md
- docs/contracts/RUNTIME.md
- scripts/parity_conformance.py
- scripts/verify_phase0.sh
- STATE.md

## Proposed answer

Record the accepted owner decisions without substituting builder choices.
Treat the relative-path failure as a builder invocation defect, not evidence
against an independent meter. Preserve the two informational cases and all
existing comparison expectations. Resolve checkpoint accounting explicitly;
never waive later checkpoints or claim clearance from a quieter tool.

Write only A-007.md and the decision log. Do not edit any other file or inspect
human/parity/check.py or exploit sources. End with a self-review listing any
assumptions about Prolog semantics. Circuit breaker: three failed edit cycles
on one test means stop and report.
