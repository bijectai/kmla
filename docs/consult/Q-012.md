# Q-012 — implementation boundary after Dev accepts A and WIRE

## Phase / lane

Phase 1, shared interface and infra. Dev has now explicitly approved A and WIRE:

> A, with two conditions: close the query-year coverage gap in Interface/ before
> any R5-group termination proof is claimed, and have gen/ never mint multibirth
> persons, multi-date births, or same-birthday eligible pairs. I accept that A
> excludes same-birthday dependent pairs from the graded domain; record it as a
> stated limitation. Install the H5 conjunct in both Valid and ValidStip; I'll
> re-pin the manifest. WIRE, including both handling proposals. Report record
> and distinct-household counts separately as proposed. Neither word signs off
> Checkpoint 1.

## Question

Review this implementation approach for the query-year condition and explicit
dependencies; do not reopen Dev's A/WIRE choice, introduce a different exclusion,
edit human/, or claim a termination proof. Write only A-012.md, not the log;
main is recording the owner's acceptance and preserving historical entries.

1. Install A's actual uniqueness/decrease predicates in shared Interface and
   include them in BOTH Valid/ValidStip for all201years1900..2100. The complete
   nonrecursive K-and-c3 eligible-pair producer does not yet exist. Make it a
   REQUIRED field on the existing OracleGuards dependency, with NO DEFAULT
   instance, no always-true/empty production implementation. Test-only mocks
   exercise wiring; they cannot certify the real predicate. Retain this concrete
   implementation obligation in STATE/HANDOFF. No oracle translation or R5 proof
   is claimed by this installation.
2. New Interface/QueryTime.lean provides a checked R5 year carrying a proof it
   belongs to the exact r5Years list checked by V10. Root-year and Workday
   constructors fail on out-of-range inputs; Workday is checked against V3 and
   converted to its actual civil year (not the enclosing taxable year). A
   covered call couples the actual source time input with that checked year and
   equality proof. R5 wrappers/proofs must take this covered input, not an
   unchecked Int or a caller-supplied bag of alleged years. Oracle signatures
   don't yet exist, so end-to-end call-site/mode completeness stays an explicit
   prerequisite. No claim that adding a standalone type proves every caller uses
   it. Stipulation-derived years/Workdays and the E2 unbound-year path must be
   accounted for as well. Is there a missing protection or an accidental new
   semantic decision in this local coverage boundary?
3. Generator implementation is a later phase and currently absent. Install its
   approved output-domain contract now: require real Valid + query-time checks,
   never mint the prohibited configurations, never inspect Oracle implementation,
   never silently drop original cases, and report record/distinct-household
   counts separately. Do not claim a generator already exists.

## What was tried / files involved

Read current Interface/Household.lean: only V7/V8 use a required OracleGuards
class; Valid has raw Year, v3 checks only fact dates. No Oracle implementations
or per-section Interface targets exist. Review accepted draft
docs/contracts/P_R5CYCLE_OPTIONS_DRAFT.md and P_WIRE_DRAFT.md, signed H5/H6,
Q/A-011. Do not rely on A-011's refuted no-K-self-edge assertion or whole-section
closure claim; the draft preserves the counterexample and qualification.

## Proposed handling

Install accepted contracts and explicit interfaces without permissive stubs.
Gate all R5 termination claims on complete producer, query-mode coverage and
kernel adequacy proofs, plus the owner's pending re-pin. Report a remaining
implementation obligation as such, not as another unresolved owner choice.
End with self-review listing Prolog assumptions and the circuit breaker:
three failed edit cycles on one test means stop and report.
