# Q-022 — list-valued original stipulations exceed the shared flat pattern type

## Phase and lane

Phase 1.1 serializer/shared representation. Checkpoint 1 is not passed.
Dev grants standing consultation through scripts/consult.sh with writes only
under docs/consult/ and append-only docs/DECISION_LOG.md. Paths are audited
before/after this call with all other repository writers paused. Contract
amendments, protected artifacts and installations remain Dev's decisions.

## Question

Does signed H4 already determine the lossless representation of proper-list
stipulation arguments, making this a shared Interface/codec implementation
gap, or is an owner amendment needed because signed G4 fixes a flat Term domain?
Identify the exact governing text and minimal correction/decision. Do not infer
permission to extend event facts, generated Valid inputs, arbitrary compound
terms, modes, projections, wildcard semantics or the semantic domain.

If the contract already determines the answer, identify precisely what shared
declaration and lossless wire shape need correction before either isolated lane
implements it. If it does not, escalate the exact owner choice and its scope;
do not accept an amendment yourself. No proposed representation is installed.

## What was tried and observed

A-021's country-only fix is independently verified on all 14 originals and
committed/pushed at 12d97e8; ordinary event traversal is unchanged. The fresh
full-population audit uses the six already implemented country paragraph modes
and tax's first solution, explicitly marking other H6 projections unimplemented.
It stopped at its 121st original, s2_a_1_B_pos.pl, during **encoding the
stipulated heads**, not source Prolog evaluation or list enumeration.

Exact original clause (source line 40, source-clause index 27):

```prolog
s151(bob,_,[charlie],[0],Year) :- between(2014,2017,Year).
```

The pinned diagnostic uses the unchanged loader and stipulated_solution/2,
prints the solutions after the existing numbervars traversal, then invokes the
unchanged encode_stips/2. Exact stdout:

```text
stipulated_head=s151(bob,'$VAR'(0),[charlie],[0],2014)
stipulated_head=s151(bob,'$VAR'(1),[charlie],[0],2015)
stipulated_head=s151(bob,'$VAR'(2),[charlie],[0],2016)
stipulated_head=s151(bob,'$VAR'(3),[charlie],[0],2017)
```

Exact stderr, exit 2, no timeout:

```text
ERROR: Unknown error term: unsupported_household_term([charlie])
```

The first failing conversion is pat_json([charlie], J), through
term_json([charlie], J0); no unbound event position is involved. Argument 3 is
the proper list [charlie] (atom element), argument 4 is [0] (integer element).
The person is atom bob, year integer 2014 on the first solution, and argument
2 is its own fresh wildcard. No list is flattened, stringified, dropped,
converted to a wildcard, or treated as a reference error/answer.

## Corpus-wide source census (not an execution-completeness claim)

An AST head census across all 376 originals and the declared 31 stipulation
signatures finds two supplied heads containing list syntax:

| Original | Kind | Exact head/clause | List positions |
| --- | --- | --- | --- |
| s2_a_1_B_pos.pl | rule | s151(bob,_,[charlie],[0],Year) :- between(2014,2017,Year) | 3, 4 |
| s63_d_2_pos.pl | fact | s151(alice,2000,[alice],_,2017) | 3 |

The visible lists have ground atom/integer elements. This syntactic census is
not proof that rules cannot produce other list structures or that these are the
only runtime list-valued solutions. Complete machine evidence and source hashes
are in docs/consult/evidence/q022-list-stipulation-census.json; the read-only
census script and pinned probe are adjacent, with their raw command records.

## Repository excerpts to reconcile

- human/DECISIONS.md G4: `Term := atom String | str String | int Int`.
- H1 retains supplied facts/stipulations in order, duplicates kept, no defaults.
- H2 describes event argument kinds and allowed event/stipulation wildcards.
- H4.1 includes s151/5 (five facts and one rule), appends stipulated solutions
  after statute solutions, and retains wildcard behavior.
- H4.2(iii) enumerates the stipulated clauses with all positions unbound,
  keeping unbound outputs as wildcards. H4.3 requires both round-trip checks.
- H6.2 explicitly encodes Prolog lists as JSON arrays at observation; this is
  not by itself an input-pattern codec or authorization to change G4.
- Interface/Household.lean declares flat Term; Pat is `val Term | wild Nat`;
  Stip stores `args : List Pat`. The outer list enumerates argument positions;
  it cannot represent a proper list **inside** one position.
- Interface/HOUSEHOLD_WIRE.md makes each Stip argument a Pat, with only `val`
  wrapping a flat Term or `wild` wrapping a Nat. It says each argument is
  preserved but supplies no list-valued position constructor.
- harness/grounding.pl's encode_stips → pattern_args → pat_json → term_json
  follows that flat shape and rejects the list rather than losing information.
- Interface/QueryTime.lean has SuppliedArg.list for query time-boundary values;
  that is not the Stip.args representation, and not a general recursive pattern.

## Proposed handling for review, not implementation

Keep the affected encoding lane paused. Preserve G4 scalar tags and the exact
stipulated argument structure/order/multiplicity and variable sharing. A list
container around existing scalar/pattern values appears capable of representing
the observed data without widening event facts, but whether that is fully
determined by approved H4/G4 is the question, not a builder decision here.
No constructor, decoder, validity rule, generated input, source or comparison
has been changed. Do not use corpus restriction or a source repair as a remedy.

## Files and retained evidence

- human/DECISIONS.md; Interface/Household.lean; Interface/HOUSEHOLD_WIRE.md;
  Interface/QueryTime.lean; Interface/WIRE.md (read-only review inputs).
- Original cases human/sara/sara/cases/s2_a_1_B_pos.pl and s63_d_2_pos.pl.
- harness/grounding.pl and harness/facts.py; scripts/test_grounding.py.
- docs/phase1/h4-post-country-evidence-2026-09-23/audit-1/summary.json,
  all-376-statuses.json, requests/ground-0241.pl, commands/245.*.
- docs/consult/evidence/q022-list-stipulation-census.json and .py;
  q022-list-stipulation-probe.pl; q022-pinned-trace/001.*.

The broad run records 120 H4.3(a) passes, one original-grounding encoding error,
255 originals blocked after that finding; H4.3(b) has one pass, 119 explicit
unimplemented projections, one blocked encoding, and 255 blocked-after-finding.
Those are run-specific statuses; the separately verified 14-country slice is
not erased. No completed all-376 claim, production records or parity result.

## Builder self-review

Assumptions to review: proper lists differ from atoms/strings and from the outer
argument tuple; s151/5's four yielded rule heads are ordered and have fresh
wildcards; encoding failure is infrastructure, not source reference-undefined
behavior. The census is syntactic, not exhaustive runtime type analysis.
Three failed edit cycles on one test means stop and report. No remedy edit or
retry of a changed encoder has been attempted. Preserve every prior A file.
