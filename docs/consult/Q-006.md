# Q-006

## Phase

Checkpoint 0 review: clarify the immutable A-005 review record only.

## Lane

infra

## Question

Please correct or clarify two statements in A-005 without editing it:

1. Its opening and rationale say four genuine open questions, while its final
   list and escalation enumerate five: 8, 9, 10, 13, and 14. Which enumeration
   governs? Keep the distinction between existing requirements, implementation
   work, and owner choices; do not invent new checkpoint prerequisites.
2. Item 13 says case-insensitive collisions cannot arise in the pinned
   `linux/amd64` runtime. The OS/CPU pin alone does not specify or verify the
   filesystem properties of input/output directories, including mounted
   directories. Please withdraw any platform-only guarantee unless the current
   contract actually guarantees those filesystem properties. We have not tested
   the eventual runner's mounts, so the review must not claim that we have.

This is a correction to the review record, not a request to choose the ID policy
or approve a runtime deviation. A-005's owner escalation remains recorded and
affected development is paused. Write only A-006, with a short answer. Do not
change any contract, prior answer, decision log, code, or protected artifact.
Never inspect the owner's meter source or exploit corpus.

## What was tried

Read A-005 and compared its summary, numbered classifications, and escalation.
The actual manifest and interface checks passed as reported; the negative
acceptance-tool reproductions remain findings. No implementation fix was made.

## Files involved

- `docs/consult/A-005.md` (immutable)
- `docs/contracts/PARITY.md` (read-only)
- `docs/contracts/RUNTIME.md` (read-only)
- `STATE.md` (read-only to Fable)

## Proposed answer

None. Clarify the count and the scope of the filesystem claim yourself.

End with a one-line self-review listing assumptions about Prolog semantics
(none should be needed); three failed edit cycles on one test means stop and
report.
