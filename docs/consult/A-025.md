# A-025

Audience: integration and both lanes; shared contracts and original sources
only. Q-025 and its analysts' conclusions are builder evidence, not owner
approval. No implementation lane was read for this review.

## Answer

### 1. Review verdict: not ready for installation

The derivation is useful retained analysis, **not yet an approved declaration**.
The following are sampled findings, not a second 135-signature audit. Evidence
paths below use `docs/consult/evidence/q025-g2-derivation/`; statute/case paths
are relative to `human/sara/sara/`.

| Item | Finding and disposition |
| --- | --- |
| `s7703_a_1/5`, position 5 under L | The table is internally inconsistent with its `s152_b_2/4` position-4 L argument. **Conditional on L and the stated corpus-stipulation premises**, position 5 is an input: any successful stipulated escape through `section152.pl:6` binds the year before `:7`; the other cited year-free routes stop before the call. `cases/s2_b_1_A_pos.pl:40` binds `Year` through `between/3`; it does not return a wildcard merely because its head contains a variable. Either make these conditional L claims consistent, or mark all conclusions affected by a broader stipulation universe unresolved. This is not selection of L or certification over arbitrary `ValidStip`. |
| Critic's “Q1 is undefined” | Stale relative to the delivered artifact. `TABLE.md:2406` explicitly defines Q1 and Q2. Preserve the critic as history, but correct the README's account of the current table. |
| `s3306_c_10_A_ii/3` L basis | The current `TABLE.md:2260` already gives the correct predecessor argument: `section3306.pl:678` cannot succeed with free Workday because `:694` reaches the `nonvar` guard at `utils.pl:11`. The README's claim that this correction is unapplied is stale. A raise at `:717`, **inside the callee**, would not establish non-entry under L. |
| Missing-basis allegations | Some are stale too: current `TABLE.md:1277` and `:2497` cite H6.5/A-024 for `s151_a/3` and `tax/3`; the tax row retains first-solution observation. The sampled §152 L rows also contain source chains. Reconcile the critic against the delivered bytes; do not mechanically apply its checklist. |

Other sampled consequences are supported: `s63_d/4` outputs 2 and 3
(`section63.pl:17,28`); `s151_b/4` and `s151_c/4` output position 2 inside
the `findall` goals (`section151.pl:36,43`); all eight `s3306_b/8` positions
remain forced by the case modes plus `section3306.pl:18,31` (A-024).
No discrepancy with those A-024 constraints was found in these samples.

The S side also needs a uniform rule for hypothetical bindings after a goal
that cannot return. “Bound on every successful return” is vacuous when there
are no returns; it cannot manufacture a binding at an unreachable successor.
S is an abstraction proposal, not an executed Prolog trace. Neither S/L
agreement nor two analysts' agreement proves the input positions universally
ground.

### 2. G2's counting universe needs Dev

G2 does not explicitly choose S versus L, or say to prune entire evaluations
using G6 before deriving the signature. G6 specifies reference definedness
and halt/report behavior, not an output-position algorithm. **I do not select
a reading.** A-024's instruction to include statute call sites did not settle
these questions.

Distinguish three policies rather than conflating them:

| Policy | Effect / cost |
| --- | --- |
| S: syntactic continuation | Includes the disputed successors even after unavoidable failure/error; requires an explicit, consistent abstract binding rule for those hypothetical successors. |
| ENTRY: operational call entry | Omits unreachable successors but counts an entered call even if it subsequently fails, raises, or belongs to an execution with no reference value; requires prefix-reachability and successful-return binding evidence. |
| Defined-domain-only | Also removes calls belonging only to excluded inputs/evaluations; requires a proved domain analysis, and changes the additional rows listed in Q-025 §2(a), not merely the ten S/L rows. |

Here is a **complete candidate clarification**, P-G2-CALLS, for Dev to approve
or amend. It proposes ENTRY and a benchmark-scoped census, not acceptance of
the existing L column:

> For the H6.5 case-mode declaration, count statute and case-test call entries
> reachable by a finite prefix of the G1-ordered evaluation from the listed
> root modes. The census covers stipulation-free, well-formed H1–H3 households
> in those modes and each original case with its own facts and H4 stipulations;
> do not pool unrelated case stipulations. Do not prune this census using
> G6/V-rule exclusions. An entered goal counts even if it fails, raises, or
> belongs to an evaluation that later raises or diverges. A successor requiring
> a return that cannot occur does not count. Binding at a successor is derived
> from the successful returns that can actually reach it, including stipulated
> rule bodies, not head-variable spelling or vacuous success implications.
> Keep H6.5's bound-answer rule. This census does not certify binding claims
> for arbitrary additional wildcard stipulations or discharge any theorem
> quantified over all `ValidStip`; those obligations remain open. No validity,
> source, observation, or corpus-population change follows from this census.

Under that candidate, §2(a)'s entered calls are **not removed merely because
their enclosing evaluation is reference-undefined**. Under §2(b), actual
original-case stipulations count: H4.1/H6.4 require them for oracle checking,
despite V1 excluding them from model grading. One may not splice an entry path
from one original into another original's stipulations to claim a live witness.
Additional wildcard environments need a separate supported-domain argument;
corpus counts alone do not settle them. Dev may instead choose a broader
census, but then the affected positions must be re-derived under it.

### 3. Answers to the remaining interpretation questions

| Question | Review answer and authority |
| --- | --- |
| Earlier statute calls bind variables | **Yes, on the successful returns reaching the later literal.** G1 is left-to-right; G2 concerns call-entry bindings, not just head bindings. `section2.pl:12 -> :18` grounds Taxp before `:13`; `section63.pl:10` grounds Taxinc before `section1.pl:51/57`. The four §2 and twenty §1 classifications still need the claimed stipulation/domain premises; this is not universal certification of those rows. |
| Beyond-corpus wildcard stipulations | **Do not extrapolate.** H4.1 permits unbound stipulated outputs; H5/`ValidStip` is not a whitelist of the 376 files. `Interface/Household.lean:388,921` does not require `s63/3` position 3 to be ground. The sixty ground original stipulations establish a corpus fact only. P-G2-CALLS makes the proposed census limit explicit without narrowing `ValidStip` or relaxing its proof obligations. |
| Calls inside `findall` and NAF | **Yes.** G1, A1 and the NAF rules still evaluate those goals. Their local bindings do not escape through NAF or through the original variables of a `findall` goal; **the third argument of `findall` does bind to the collected list**. Correct any unqualified “findall binds nothing outside” premise in the critic. Exceptions are not ordinary NAF success. |
| Guard-emptied calls | **The entered call counts.** `section152.pl:5 -> :39 -> :3` enters the wrapper and then the guarded `s152` call; no body literal beyond the failing guard executes. R4's specialization sketch does not erase the source call or change `s152_b_1/3` position 1's free occurrence. |
| `==` with an unbound side | **At the cited site, yes:** a free `S65` cannot be identical to the ground `Bsssc2b` supplied by kinship facts at `section152.pl:180–184` (G3/G4). This is not a blanket rule about two unbound terms: the same variable is identical to itself; A3 preserves wildcard identity. Also do not equate `nonvar` with `ground` for partially instantiated lists. |
| Harness-created calls | **Not extra G2 source call sites.** H4.2 grounding and H6.2 observation are separate obligations. This avoids circularly deriving the projection from its own observation wrapper; it does not waive support/coverage for the actual calls those wrappers make. |
| F10's transitive route | **Yes.** `section68.pl:75 -> section2.pl:5 -> :12` supplies a free taxpayer to `s2_a_1_A/5`; G9/F10 expressly require literal translation. No repair to the surviving-spouse argument is authorized. |
| `s63_d_2/3` position 2 | **Input for this census.** The literal lists are bound at the two original tests; the question asks whether the deduction falls under the paragraph, not for the list as an answer (`cases/s63_d_2_pos.pl:5,17`, negative counterpart). A targeted statute search finds only the definition at `section63.pl:303`, no statute caller. This follows the evidence and H6.5, not omission from A-024's non-exhaustive table. |
| Stipulated rules binding a head variable | **Yes.** Successful `between(2004,2019,Year)` binds Year in `cases/s2_b_1_A_pos.pl:40`; H4.2(iii) preserves the resulting solutions. Merely having a variable in the source head does not mean an unbound output. H4.1 order still matters: an exception/divergence in an earlier statute clause is not bypassed by a later stipulated clause. |

### 4. Representation

The proposed one-based `outputs` map keyed by case mode, separate generated
Lean function, and H6.5-only initial keys are suitable builder-owned spelling
choices. Preserve predicate **and arity**, explicit absence versus `[]`, valid
position ranges, and tuple order. Internal modes remain evidence/translation
obligations. An empty output-position list is not permission to emit Boolean
JSON: non-scalar H6.2 observations still use solution arrays (zero-tuple success
`[[]]`, failure `[]`). No unresolved position becomes a default. Representation
agreement is not approval of either content column.

### 5. Findings for Dev

**Citation slip: confirmed, editorial.** G2's `(R3)` at DECISIONS line 40 should
reference `(R4)` at lines 1394–1401, not `amount/2`. Proposed owner correction
recorded with P-G2-CALLS; the guard's substantive instruction is already clear.

**Two corpus anomalies: confirmed source/question discrepancies, not builder
defects or authority to repair.** `cases/s151_b_neg.pl:20` uses `/3` with atom
`bob` in the amount position (`section151.pl:84`), while its question describes
a spouse exemption. `cases/tax_case_100.pl:5,28` names Bob in the question but
queries Alice. Preserve the exact predicates, arities, tags, queries and labels
under G9/H6. No exclusion, coercion, query substitution or stronger paper claim
follows; Dev owns any corrected-reference experiment.

**Possible G6/V-rule gap: credible, unconfirmed.** The inspected source supports
the proposed entry path `utils.pl:342 -> section3301.pl:7 ->
section3306.pl:14 -> :57 -> :44 -> :441 -> :604`. At `:44`, both Workday and
Caly are free; the var branch at `:428` does not bind Caly. Domestic-service
rejection at `:604` can therefore invoke `s3306_a_3` with free Caly.

A useful diagnostic candidate is an **unpaid** domestic service with employee
and employer, and string locations `"private home"` and `"usa"`, with no
payments, plans, kinship, births, residences or stipulations. The payment
relation in V7 is then empty. Static expectation: with no payments the first
wage branch fails, and `Pyear is Caly-1` at `:242` is reached with Caly free.
This is a candidate, **not a pinned reproduction or a certified Valid witness**.
The table's universal wording “always raises at :222 or :242” also needs care:
`:220` calls into the known recursive group first; on other households it can
fail to return before either arithmetic/date site is reached.

Require a bounded diagnostic on the pinned runtime, with the exact root query,
facts, runtime identity/TZ, call goal and bindings, stdout/stderr verbatim,
exit/timeout status and each signed V1–V10 premise checked without fake guards.
Check the actual root behavior, not just an isolated raising helper. Do not
claim V7 passes merely from an unfinished production decider. Preserve a
disproving result too. If the candidate is admitted by the signed rules and
raises, that is a **reference-definedness contract gap**, not an oracle parity
defect; return to Dev with a minimal exclusion proposal and its effect on the
originals. No V-rule is approved in advance, and no fuel/exception-as-value,
source repair, input filter or weakened comparison is allowed.

## Rationale

G1/G2 govern call ordering and bindings; H4 and H6.4 keep original stipulations
in oracle checking; G6 does not prove the V-rules complete; G9 prohibits fixing
source intent. I sampled the retained derivation and cited original clauses,
not either lane's implementation. No interpreter, full corpus audit, evidence
re-hash, meter interaction or implementation test was run.

## Contract change

Yes — **PROPOSED only**, P-G2-CALLS: explicit counting/census scope and the
editorial R3-to-R4 citation correction. P-H6-CONJ remains pending unchanged.
R-Q025 records the sampled evidence corrections, corpus anomalies and
unconfirmed G6 finding; it installs no remedy.

## Escalate to Dev

Yes — approve/amend P-G2-CALLS and resolve the existing P-H6-CONJ hold; receive
the G6 finding and its required bounded diagnostic before any domain amendment.
The builder must record these in STATE and keep dependent declaration,
producer/scoring and affected §3306/§3301/`tax` definedness work halted.
Source analysis and bounded diagnosis are distinct from implementing a remedy;
this answer dispatches no lane and signs no checkpoint. Audit changed paths
at delivery; an out-of-scope write stops the lane.

## Self-review

Assumptions: G1 SLD order, successful-return bindings, H4 appended stipulations, NAF exception propagation and G3/A3 identity as cited; the proposed census and runtime candidate remain unapproved/unmeasured respectively; three failed edit cycles on one check means stop and report, no fourth attempt or weakened check (none attempted here).
