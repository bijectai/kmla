# Q-005

## Phase

Checkpoint 0 integration review; no Phase 1 implementation is authorized by
this question. The independent parity meter remains absent and human-owned.

## Lane

infra

## Question

The owner supplied a consolidation update and pointed to fourteen purported
open questions in `scripts/parity_conformance.py --list-gaps`. Classify each
item against the actual `docs/contracts/PARITY.md`: already specified,
implementation/integration responsibility, or a genuine unresolved contract
choice requiring Dev. Give the governing wording for already-specified items.
Do not silently turn the list into fourteen new checkpoint requirements, and
do not propose or apply a new contract in this consultation.

In particular, the existing contract already names the report location, exit 2
for invalid schema/malformed records or inability to compare, and the selected
input population. Which questions genuinely remain after respecting those
clauses? Distinguish a crash/stale-report safety problem from ordinary malformed
input handling. Is correcting the reproduced builder-tool defects below a
restoration of existing contracts rather than a human-semantic decision?

The current protected manifest verifies on the actual tree, and the interface
type-checks on 4.33.1 with 29 guards. This does not establish that the checkers
reject invalid input, nor that the unresolved V7/V8 oracle guards are implemented.
The original 29 guards use a test-only always-true OracleGuards instance.

Only write the new A-005 for this review. Do not edit prior A files, the decision
log, README, contracts, scripts, or any protected artifact. Never inspect the
parity meter source or the exploit corpus. No runtime-deviation approval or
checkpoint waiver is being requested from you; both remain Dev's authority.

## What was tried

1. SHA-256 of the working and committed `human/DECISIONS.md` both equals
   `58dad769c0a318e018275dc106b1389e0ac7d89586e0c66bb33f6d8f3e19b30c`.
2. `bash scripts/check_interface.sh` passes with 29 behavioural guards.
3. `python3 -B scripts/human_manifest.py verify` passes on the current tree.
4. Imported the builder's conformance harness with `runpy.run_path`, called
   `agreeing()` in a temporary directory, and examined only its generated
   fixture. The purported valid input is
   `{"schema_version":1,"input_id":"s151_a_pos","section":151,"result":null}`.
   It lacks the `payload` required by PARITY.md. A meter enforcing the input
   envelope could correctly reject the supposed all-agree test.
5. Imported the builder's manifest helper into a separate temporary-fixture
   test. A symlink to a directory is silently omitted by `walk_protected()`;
   generation followed by verification returns no problems. A separate fixture
   with `nested/HASHES.txt` likewise generates no record for it and verifies
   successfully. HASHES.md forbids symlinks and excludes the root manifest
   itself, not every file with the same basename. No file under the real
   `human/` was created, modified, removed, or chmodded in these tests.

Affected acceptance tooling is paused pending review. No implementation fix,
contract amendment, migration, commit, or push has been performed in this review.

## Files involved

- `docs/contracts/PARITY.md`
- `scripts/parity_conformance.py` (builder test harness, not the owner meter)
- `docs/contracts/HASHES.md`
- `scripts/human_manifest.py` (builder manifest helper)
- `STATE.md`
- `docs/DECISION_LOG.md` (read-only acceptance status; P-RUNTIME/P-SUMLIST pending)
- `Interface/Household.lean` and `scripts/interface_guards.lean.inc` (read-only)

## Proposed answer

None for the open contract classifications; answer independently from the
written contracts. The reproductions above are findings, not a revised design.

End with a one-line self-review listing any assumption about Prolog semantics
(none should be needed) and the circuit breaker: three failed edit cycles on
one test means stop and report.
