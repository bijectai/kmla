# Q-003

## Phase

Infrastructure: consult-channel answer-consistency smoke test only.

## Lane

infra

## Question

Please clarify two passages in immutable A-001 against the approved B003/B004
text in `docs/PROTOCOL.md` and its summary in `docs/HANDOFF.md`:

1. "They never share inputs" and witnesses are never "regenerated into
   `gen/out/`." Does the approved rule prohibit admission-witness leakage into
   grading, or also prohibit independently generated inputs from coinciding in
   value? B003 explicitly permits approved missing input modes in a separately
   frozen v2, with the same admitted mutant set. It specifies independent
   searches and seeds, not disjoint sets of realized input values.
2. Witnesses are "never into the invariant-refutation corpus." Where is this
   blanket restriction established? B004 permits candidate counterexamples from
   the frozen corpus plus witness search over the statement's quantified
   variables, and requires a kernel-checked counterexample for REFUTED. The
   approved text does not state that provenance alone invalidates such a
   candidate, although admission evidence is not a differential kill.

Are these passages overstatements in your restatement, or are you proposing new
contract restrictions? If overstatements, correct them in A-003 and identify the
specific A-001 wording it supersedes. Do not edit A-001 or any earlier answer.
Do not silently promote reporting guidance to an approved contract.

No KMLA phase work is authorized. Write only the new A-003 for this smoke test.
If you believe a new contract or a write outside `docs/consult/` is required,
report that need and escalate instead of making that change in this test.
End with a one-line self-review.

## What was tried

Q-001 elicited the B003 restatement. Q-002 successfully recalled Q-001 and its
receipt from the resumed conversation without rereading the earlier Q or A.
The builder's consistency review found the two passages above were not explicit
in the approved amendments. No KMLA implementation or contract has been changed
to follow those passages.

## Files involved

- `docs/consult/A-001.md` (read-only)
- `docs/consult/Q-003.md`
- `docs/PROTOCOL.md`, B003 and B004 (read-only)
- `docs/HANDOFF.md`, Confirmed later-phase rules (read-only)

## Proposed answer

None. Please distinguish the approved requirements from any extra restriction
in your earlier answer, and resolve the consistency check in your own words.
