# Q-023 — H4 re-grounding multiplies repeated-event facts

## Phase and lane

Phase 1.1, serializer. Affected grounding development is halted at a measured
H4.3(a) failure. Dev's standing consultation authorization applies. Write only
this answer under `docs/consult/` and an appended review/proposal entry in
`docs/DECISION_LOG.md`. Do not change old answers, implementation, comparisons,
protected artifacts or this question. Nothing here authorizes installation.

## Question

Classify the failure below against signed H1/H4.2/H4.3/G1/G4. Is there an exact,
already-determined implementation correction, or does resolving the conflict
between traversal multiplicity and re-grounding identity require Dev's decision?
If an amendment is needed, propose precise minimal text, explain what it changes
and escalate; do not accept it. No source repair, validity exclusion, reordered
comparison, deduplication of stored facts/solutions or waived original is allowed.

In particular, distinguish the **event domain traversed by step (ii)** from
the ordered unary facts themselves and from multiple binary proofs. Does the
signed word "universe" already determine distinct events and their order, or
would choosing that now reinterpret a missing clause? Do not silently decide
this. Account for the original bodyless-event order and genuine duplicate
clauses/proofs under whichever classification you reach.

Dev's latest country instruction is still binding: "do not bypass step (ii)
for bodyless event facts generally, since step (ii)'s per-event order is what H1
fixes for those." A-021 also explicitly did not authorize deduplicating the
outer event traversal. The staged A-020 tax33 candidate keeps the original
outer traversal and a separately distinct service domain. Explain whether
the present finding requires revisiting any of those commitments, not just
changing a loop until this test passes.

## Exact finding and what was tried

The authorized recursive-stipulation correction passed its two original H4
regressions and the 14-country slice. A fresh broad audit stopped on attempt
149 at `human/sara/sara/cases/s2_b_3_B_pos.pl`, SHA-256
`cdf109a726f44e6b5aced16ad84354a71ef51bd8b1bc3e8899b4fb81f9ac7f8a`.
Its original source query is `s2_b_3_B(bob,charlie,2018)`. No list-valued
stipulation failure occurs here. H4.3(b) for this signature is unimplemented;
no semantic-output mismatch is claimed.

Both initial grounding and re-grounding exit 0, without timeout. H4.3(a) fails:

```json
{"field":"facts","index_zero_based":20,"original_grounding":{"ctor":"agent_","args":[{"a":"bob_maintains_household_2016"},{"a":"bob"}]},"regrounding":{"ctor":"agent_","args":[{"a":"bob_maintains_household_2015"},{"a":"bob"}]},"original_length":114,"regrounded_length":204}
```

The five stipulations remain equal. Main independently reproduced the same
failure with unchanged code and performed one further diagnostic re-grounding:
fact counts **114 → 204 → 384**. Each pass has 13 unary proofs over 8 distinct
ground event terms. Each of the five maintenance event atoms occurs twice,
under `payment_` and `income_`; the other three occur once. No alternative
traversal, source, domain, serializer or comparison was tested.

Source lines 29 and 42 really read:

```prolog
payment_(Event) :- bob_household_maintenance(_,Event,_,_).
income_(Event) :- bob_household_maintenance(_,Event,_,_).
```

There are also two rule clauses producing `agent_(Event,bob)` from that same
helper. The unused `bob_income` helper does **not** authorize correcting the
source to what its author may have intended (P-INTENT).

Current code (`harness/grounding.pl`) enumerates unary source clauses and
their proofs into `Unary`, projects every event occurrence into `Events`
without deduplication, traverses each binary source clause over `Events`, then
stable-sorts emitted facts by original clause provenance. Serialized facts
are ordinary clauses on the second pass, and the same traversal is applied.
Inference, not a revised design: the repeated event-domain occurrences appear
to multiply already-materialized binary proofs on each pass.

## Files involved / evidence to read

- `human/DECISIONS.md`: G1/G4/G5, H1–H4, P-INTENT; read-only.
- `human/sara/sara/cases/s2_b_3_B_pos.pl`: complete original, read-only.
- `harness/grounding.pl`: `run`, `unary_solution`, `binary_solution` and ordering.
- `harness/grounding.py`: original and re-grounding calls; no oracle code needed.
- `harness/facts.py`: `emit_prolog`, ordered fact serialization.
- `docs/contracts/H4_BOUND_PURPOSE_DRAFT.md` and immutable A-020/A-021: prior
  scoped candidate and traversal constraints.
- `docs/phase1/stip-list-harness-evidence-2026-09-23/first-witness.json` and
  `full-audit/s2_b_3_B_pos.status.json`: first mismatch, counts and provenance.
- `docs/consult/evidence/q023-reproduction-2026-09-23/summary.json`: independent
  three-pass reproduction, before/after code hashes and event counts.
  Adjacent `ground-0001.measurement.json` through `ground-0003.measurement.json`,
  `requests/` and `commands/` retain complete programs/results/argv/streams.

Runtime is the unchanged measured SWI 7.2.3 Debian `7.2.3+dfsg-6`, linux/amd64,
America/New_York; immutable image
`sha256:8e53d3a0003635786ccdeafc0048b3344b621815fe4881fd4512914a95606ec7`,
full identity `744cbb885225c77569618a35c4c856f5c4a6c6e4fb6ecb37f7476eb46bbf5c92`.
Both `human/` and corpus are mounted read-only. All 376 remain accounted for:
148 H4(a) passes, 1 failure, 227 unvisited; H4(b) 3 passes, 145 unimplemented,
1 blocked by this mismatch, 227 unvisited. Separate successes are not imported
into this stopped audit. 149 diagnostic Households / 93 distinct; production
records are not yet produced. No parity or Checkpoint 1 result is claimed.

## Proposed answer, if any

None selected. Please distinguish an authorized repair from a missing owner
choice with exact citations. If Dev must decide, make the proposal explicit
and state required regression evidence, including preserved duplicate clauses,
multi-kind events, ordering, both H4 comparisons, the 14-country regressions
and the scoped tax33 candidate. Do not recommend weakening H4.3.

## Self-review and circuit breaker

Assumptions under review: event-domain multiplicity is not automatically the
same thing as source clause/proof multiplicity; the source's repeated helper
calls are literal semantics, not typos we may fix. The unchanged pinned runs
establish the failure, not a corrected design. Zero remedy edits have been
attempted. Three failed edit cycles on one test means stop and report; no fourth
attempt, changed expectation or weakened assertion. End your answer with your
own Prolog assumptions and circuit-breaker self-review.
