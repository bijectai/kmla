# Q-011 — Draft-only owner menu for P-WIRE and P-R5CYCLE

## Phase / lane

Phase 1 hold; infra/design review only. Dev explicitly requests a draft reduction
to owner choices, no recommendation, no installation and no decision by builder.
Write only A-011.md for this consult. Do not change DECISION_LOG.md, any operative
contract, Interface, STATE, or human/. Existing escalations remain operative.

## Question

Review the precise proposed P-WIRE wording and the three candidate R5 routes
below for logical defects, especially mode/binding, stipulation and fuel issues.
Do not recommend or approve any route. This is preparation for Dev's choice.

P-WIRE replacement for the pending-definition sentence (with the preceding
future-tense sentence brought into the same replacement span):

> Payload and result semantics are supplied by the approved
> `human/DECISIONS.md` H1–H4, G4, D1/V9, M1, H6.1, H6.2 and H6.5 and the
> corresponding `Interface/` declarations. Their concrete JSON spelling is a
> builder-owned lossless codec of that approved shape, fixed once in
> `Interface/` and shared by both isolated lanes; the independent meter needs
> only input identity and section to determine the expected output population.

Replacement for the blocked-producers sentence:

> Producers may encode fields determined by those approved sections using the
> shared lossless codec; producers remain blocked on any field those sections
> do not determine and must obtain a recorded owner decision before supplying it.

The preceding requirement to implement only approved canonicalization remains.
Separate owner choices in the same approval: one record per (Household, target,
declared mode, bound-argument tuple including year), with original-case identity
retained independently and original case success still H6.3; compact UTF-8,
existing H6.2 tags and order, short b/f/n/r/t escapes, lower-case u00xx otherwise,
raw other unicode, no normalization, integer decimal without float conversion,
as existing shared observation encoder. Do not change mode, projection or
canonicalization semantics. Kill denominator remains admitted mutants, NOT
number of records (A-008's counting-unit phrasing needs this qualification).

R5 option candidates (all are uninstalled, with corpus impact being audited):

Let U be a finite superset of all possible ground person Terms in the Household
and original case stipulations; preserve tag-sensitive equality. Include every
participant; count distinct members, not fact multiplicity. N = |U|. Fix y.
V4 already ensures child descent terminates. Let K(T,D) hold when the original
`s152_c_2(D,T,_,_)` has any solution with both people bound, keeping every date
choice and original clause ordering/identity tests. This includes stipulated
c2 clauses for the original-case ValidStip audit. K may have self edges.

* A (birth-function narrowing): at most one distinct birth event per person and
  at most one distinct start day per such event. Duplicated identical clauses
  are not deleted or counted as different events/days. For EVERY K(T,D) with
  `s152_c_3(D,T,y)` succeeding, require either (i) D has a birth event and both
  T and D have defined unique birth days, with dob(T) < dob(D), or (ii) D has no
  birth event and D is a strict descendant of T. This extra V-rule is a domain
  restriction, NEVER a source change to is_before. It covers c3 stipulations
  too, rather than assuming they obey statute age order. Acyclicity argument:
  born-to-born strictly increases the unique date; once a path reaches a
  birthless person it cannot return to a born person; birthless-to-birthless
  follows the V4 child DAG. Self edges fail both alternatives. Every recursive
  R5 transition is a subset of K-and-age. Hence no more than N distinct person
  anchors on a branch. Candidate fuel: N+1 with decrement ONLY once when an
  s152_c_1 body crosses from its successful A/B/C prefix into c1E's s7703 call;
  helper calls don't decrement that counter; R8 shares rather than resets it.
  Need wrapper/mode accounting for direct entries before claiming this bound.
* B (formal all-solution graph): E_y(T,D) iff some solution of the actual
  c1A(D,T,S,E), c1B(D,_,T,S,E,y), c1C(D,T,y) prefix succeeds, sharing S/E and all
  variable bindings exactly as the source. Require acyclicity of E_y, not
  merely V4. No uniqueness. Rank = longest E_y path; prove every recursive
  transition covered, finite enumeration and decidability including modes and
  ValidStip, derive counter from rank and a precisely fixed decrement site.
  This narrows Valid too: a proof cannot make the two existing cycles terminate.
  Choosing B authorizes the proof work, not immediate use of unproved oracle.
* C (coarse structural exclusion): require K acyclic, including self edges,
  ignoring year/residence/age/marriage conditions. No birth uniqueness. E_y is
  a subset, so a topological rank for K bounds the same recursive transitions.
  This knowingly excludes terminating households, notably some siblings;
  exact original-case exclusions will be shown, never silently removed.

For each: does the source support the macro-decrement site and N+1, including
top-level c1E, bound/free source modes, R8, appended stipulations? Identify exact
remaining proof obligations; do not certify a kernel proof that does not exist.
Check BOTH retained fixtures, not just one: the equal-birthday sibling and the
multibirth case. Counter-budget exhaustion alone is not a divergence proof.
State which halted work resumes after selection vs after owner installation and
proof. No implementation or signed-domain changes are authorized in this turn.

## What was tried / files involved

Read original section152.pl:57–230, section7703.pl:2–139, utils.pl:98–187;
signed G1–G6/H1–H6/R5/R8/R9; A-008, A-009 and A-010; both diagnostic fixtures.
The current source c3 has no disability disjunct. The first c3 disjunct binds
Individual_is_born/Individual_dob reused by the final under-25 conjunct; A-010's
claim of independent reselection there is too broad (STATE already qualifies it).
Across recursive invocations, fact selections remain independent existential
choices. Existing source/meter/runtime are unchanged.

## Proposed answer

No approval is proposed. Audit the menu without recommendation, surface any
counterexample or overclaim, and leave Dev the choice. No claim that the corpus
has zero exclusions until the pinned audit completes. End with a self-review
listing assumptions about Prolog semantics and the circuit breaker: three failed
edit cycles on one test means stop and report.
