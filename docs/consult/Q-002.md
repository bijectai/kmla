# Q-002

## Phase

Infrastructure: synchronous consult-channel session-resume smoke test.

## Lane

infra

## Question

From this conversation's memory, what did Q-001 ask you? Summarize that earlier
question and recall the exact arbitrary receipt label it included. Do not read
Q-001.md, A-001.md, transcripts, memory files, or search files to recover this
information. If it is absent from your conversation context, say so explicitly;
do not guess or infer it from the standing project prompt.

You may read this question and any mandatory orientation documents. This tests
whether `--resume` preserves the actual first conversation, not whether the
same information can be retrieved from disk. No KMLA phase work is authorized.

## What was tried

Q-001 produced A-001.md and successful CLI JSON. The first wrapper invocation
then encountered a macOS chmod argument-order bug before saving its session ID.
The builder fixed the chmod invocation and restored the verified session ID
from that retained CLI response, preserving A-001. This call must resume that
exact conversation and exercise the corrected script's response handling.

## Files involved

- `docs/consult/Q-002.md`
- `docs/consult/.fable_session` (managed by the wrapper; do not edit)
- `scripts/consult.sh` (context only; do not edit)
- Q-001 and A-001 are references only; do not reopen or search them for this test.

## Proposed answer

None. Recall the earlier question and its receipt label from conversation state.
