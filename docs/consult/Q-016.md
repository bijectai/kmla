# Q-016 — resume the concrete query-admission kernel proof

Phase: 1 entry. Lane: infra/shared Interface proof only.

Dev has now said "continue with phase 1", in reply to the circuit-breaker
report and request to resume with a fresh proof review. The prior stop remains
recorded; this is the authorized resumed review, not a fourth unreported retry.

## Question

How should we diagnose and prove the **same** concrete impossibility claim in
`scripts/query_time_guards.lean`, without changing approved semantics, weakening
or removing the assertion, native evaluation, or added axioms?

## What was tried

The three prior failed proof edits (change, decide, decide +kernel) and exact
diagnostics are in
`docs/phase1/QUERY_ADMISSION_CIRCUIT_BREAKER_2026-09-22.txt`.
Unmodified reproduction at b80a807 still fails:

```text
Tactic `decide` failed for proposition
  emptyH.v3ForQuery 2015 (r8 (day "2101-01-01")) = false
because its `Decidable` instance did not reduce to `isTrue` or `isFalse`.
```

All preceding executable guards reject the bad query, but that is not a kernel
proof. The generic certificate projection theorem compiles. Main is isolating
reduction of `queryTimeSchema?`, `Day.fromISO?`, `String.splitOn` and `toInt?`
in temporary diagnostic files before editing the assertion again.

## Files / proposed handling

Read Interface/Household.lean, QuerySchema.lean, QueryTime.lean and the guard
file. No Oracle or serializer implementation is involved or to be inspected.
The suspected issue is a reduction boundary in string parsing or generated
lookup, not a wrong date bound. Prefer a proof using library correctness lemmas
or equivalent kernel-reducible shared plumbing with an equivalence proof; do
not choose changed input semantics merely to make decide work. No reference or
R5 termination claim is sought. `human/` remains read-only and the meter's
source must never be read. Write A-016 only unless a real contract change must
be proposed. Report assumptions and whether anything actually needs Dev.

End with a self-review listing assumptions about Prolog semantics and the
circuit breaker: three failed edit cycles on one test means stop and report.
