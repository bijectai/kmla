# Q-017 — H4.2's event-bound grounding call raises in tax_case_33

## Phase

Phase 1, after Checkpoint 0 owner sign-off and A/WIRE installation at 0a2a65a.
Actual query/stipulation V3 preflight now passes. Neither Checkpoint 1 nor a
general H4 round-trip result is claimed.

## Lane

serializer (harness grounding). The affected path is halted. Independent
nonrecursive oracle slice checks may finish; no alternative grounding design
is being installed.

## Question

Does an existing signed decision authorize a lossless correction to this
grounding procedure, or does H4.2 need an owner-approved amendment? Identify
the smallest precise decision Dev must make, with the preservation/proof
obligations. Do not silently select an event-pair enumeration or reorder a rule.
Do not infer that a successful ground original query permits an exception in
the grounding procedure to be represented as an empty list.

H4.2 in human/DECISIONS.md says enumerate unary event predicates for the event
universe, then every binary event predicate with only its event position bound,
preserving all solutions and multiplicity. It specifically says a grounding
that raises or does not terminate is a finding. In original
human/sara/sara/cases/tax_case_33.pl:29–31, purpose_/2 runs split_string/4 on
both Payment_event AND Service_event. The prescribed first-position-only
binding does not make that second split callable.

## What was tried

The isolated serializer context ran its exact H4 diagnostic on immutable image
sha256:8e53d3a0003635786ccdeafc0048b3344b621815fe4881fd4512914a95606ec7,
with full RUNTIME.json identity checked, /human and /corpus read-only. It loaded
the original reader-approved clauses after init without case directives,
enumerated payment_(E), selected the first event, and called:

```prolog
findall(Purpose, purpose_(Event, Purpose), Purposes)
```

No second-position binding, argument swap, source repair or input exclusion was
attempted. The retained process exits 2 without timeout. Its stdout, verbatim:

```text
{"payment_solutions":157}
payment_2015_1
```

Its stderr, verbatim:

```text
ERROR: split_string/4: Arguments are not sufficiently instantiated
```

The coordinating task independently read the retained process metadata/streams
and original clause locations. This is a grounding failure, not a Lean/Prolog
parity mismatch. The original complete case's earlier directive baseline passes;
the calling mode here differs.

## Files involved

- human/DECISIONS.md: H4.2–H4.3, G1/G2/G6, H1–H3, P-INTENT.
- human/sara/sara/cases/tax_case_33.pl:29–31 (read-only).
- docs/phase1/harness-evidence-2026-09-23/h4-probe-docker/commands/005.command.json
- docs/phase1/harness-evidence-2026-09-23/h4-probe-docker/commands/005.stdout
- docs/phase1/harness-evidence-2026-09-23/h4-probe-docker/commands/005.stderr
- docs/phase1/harness-evidence-2026-09-23/h4-probe-docker/identity.json

No need to inspect either implementation to decide this contract issue. Never
inspect the owner meter/exploits or write human/. Answer only in A-017; if a
contract amendment is required, append a PROPOSED entry to DECISION_LOG, do
not mark it accepted or install it.

## Proposed answer, if any

No remedy selected. This appears to refute H4.2's sufficiency claim, not G6 for
the original admitted query. Any replacement needs completeness, original
source solution order/multiplicity, wildcard preservation and both H4.3 checks;
enumerating an arbitrary Cartesian product is not by itself that argument.

End with self-review: assumptions about Prolog semantics and the circuit
breaker (three failed edit cycles on one test means stop and report).
