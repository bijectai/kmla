# Q-009: R5 sibling cycle contradicts the stated termination argument

## Phase

Phase 1, authorized after explicit Checkpoint 0 sign-off. Development is paused
per Dev's instruction to pause and ask when clarification is required.

## Lane

oracle / shared semantic specification. This supplies original-source evidence
and a diagnostic input, not either lane's implementation. No Oracle code exists.

## Question

Does this invalidate R5/R9's assertion that the §7703/§152 group only follows
child edges and cannot exhaust person-count fuel under current V1-V9? Identify
any missed signed condition or source conjunct that would reject the input.
If it is a genuine missing domain/termination decision, provide the narrow
owner decision needed and a recommended conservative formulation for Dev to
review. Do not edit human/ or silently choose fuel, change clause evaluation,
exclude an input, or make a new rule operative. The owner, not Fable or Astra,
must adopt semantic amendments. Distinguish bounded runtime evidence from a
formal proof of divergence or `Valid`.

## What was tried

The fresh §7703 context statically identified this path in the original source:

```text
s7703(a,sa,_,2018)
  -> s7703_b_1(a,home,b,2018)
  -> s152_a_1(b,a,2018) -> s152_c(b,a,2018)
  -> s152_c_1_E(b,_,2018) -> s7703(b,sb,_,2018)
  -> s7703_b_1(b,home,a,2018)
  -> s152_a_1(a,b,2018) -> s152_c(a,b,2018)
  -> s152_c_1_E(a,_,2018) -> s7703(a,sa,_,2018)
```

R5 says each turn follows a child relation. But §152(c)(2)(B) also accepts
siblings, `is_sibling_of` is symmetric, and the age-order condition uses
non-strict `is_before`, admitting equal birth dates.

The orchestrator reproduced the candidate on the pinned Debian
`swi-prolog-nox=7.2.3+dfsg-6`, linux/amd64, America/New_York image, whose measured
whole-record identity matches the current local RUNTIME.json (except local ID).
The fixture is `docs/consult/evidence/r5_sibling_cycle.pl`; all mounts, including
the original corpus and diagnostic fixture, were read-only. No original case
or source was changed. The fixture has two equally aged siblings, separate
ongoing marriages, and a shared residence. No child/parent facts, payments,
joint returns, services, plans or stipulations.

Retained result: `docs/consult/evidence/r5_sibling_cycle_results.json`.
Each command used a fresh process and retained raw stdout/stderr, exit and budget:

```text
finite_marriage_prefix 0 'prefix(sa,marriage_a)\n' ''
sibling_cycle_100k 0 'inference_limit_exceeded\n' ''
sibling_cycle_1m 0 'inference_limit_exceeded\n' ''
no_sibling_control 0 'control(!,sa,marriage_a)\n' ''
```

The last control retracts only `brother_` in memory in that fresh diagnostic
process; it is not a proposed source change. Dynamic declaration in the fixture
exists only to allow this named control. Inference exhaustion is **not** an
ordinary empty answer or a parity result. No production OracleGuards exists,
so no kernel-checked `Valid` certificate is claimed.

## Source and specification obligations

- R5/R9, human/DECISIONS.md:1361-1393.
- §7703:103-139 reaches the dependent recursion before the payment check.
- §152:143-153 calls `s7703` inside NAF before testing joint-return facts.
- §152:178-186 admits siblings; utils:131-155 makes the relationship symmetric.
- §152:192-230 and utils:10-14 accept equal birthdays.
- V4 has an empty parent graph for this input; no services/plans trigger V5-V7.
  V8's stated exceptional year condition is false in 2018. These are source/spec
  deductions, not a completed production validity check.

## Files involved

human/DECISIONS.md; original section7703.pl, section152.pl and utils.pl;
the two diagnostic files above; STATE.md; docs/PROTOCOL.md.
Do not read human/parity/check.py or serializer/Oracle implementation.

## Proposed answer

This appears to be precisely R9's design-flaw case: the signed argument does not
justify a total semantics on the stated domain. Halt the recursive closure and
dependent production work, preserve the witness and ask Dev for a revised
termination/domain decision. Do not replace exhaustion by [] as a purported
reference value. Please review whether a narrow, decidable conservative cycle
guard can be specified without changing the original source's literal behavior;
any proposed restriction stays proposed until Dev accepts and installs it.
