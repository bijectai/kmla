# A-024

Audience: integration and both implementation lanes; shared-contract/source
review only. No lane implementation was read for this answer. Q-024 is builder
evidence, not owner authorization. Checkpoint 1 is not passed.

## Answer

### 1. Declaration location and scope

Option (a) is a reasonable builder-owned representation: an explicit per-mode
output-position field alongside the case modes in `Interface/TIME_SCHEMA.json`,
rendered separately from time roles into `QuerySchema.lean`. Neither G2 nor H6
requires a separate file. Declare the indexing convention; distinguish a
verified empty output tuple from an undeclared projection. Completeness and
generation checks do not establish that the selected positions are correct.

The initial declaration can cover exactly H6.5's 135 signatures and listed case
modes. **That does not restrict the evidence used to determine their outputs to
case calls.** G2 explicitly includes statute call sites. Adding statute-internal
mode functions later, as H6.5 directs, is not permission to disregard those call
sites now. WIRE authorizes spelling, not a new projection policy.

### 2. Outputs: determined rules versus unfinished evidence

Positions below are one-based. Apply G2 and H6.5 together:

- A position free at a relevant statute or case call site is an output under
  G2; a bound occurrence elsewhere does not erase it. Bound output occurrences
  impose G2's equality filter. Preserve the invocation mode, including its
  instantiation guards; an output annotation does not license changing a call's
  bindings.
- Also include a bound position explicitly presented as the answer by the case
  question (H6.5). Do not infer that every number, identifier, year, or final
  argument is an answer. Conversely, a question phrased as “applies” does not
  erase outputs established at other call sites.
- An input classification needs the **every-call-site** evidence G2 requires.
  A position not mentioned in the following table is not thereby an input.
  Nor may an unresolved row default to the positions marked `f` in that row.

The retained question inventory supports these **required bound-answer output
positions**, for the signatures' listed case modes; these are additions to,
not replacements for, G2's outputs:

| Signatures (brace notation means each named combination) | Required position |
| --- | ---: |
| `s1_{a,b,c,d}_{i,ii,iii,iv,v}/2` | 2 |
| `s1_a/4`, `s1_b/4`, `s1_c/4` | 4 |
| `s1_d/5` | 5 |
| `s63_a/5`, `s63_b/4` | 3 |
| `s63_c_1/3`, `s63_c_2_B/3`, `s63_f_3/3` | 3 |
| `s63_c_2_C/2`, `s63_c_7_i/2`, `s63_c_7_ii/2` | 2 |
| `s63_c_3/3` | 2 |
| `s63_c_5/5` | 5 |
| `s68_a_2/4` | 3 |
| `s68_b_1_A/5` | 4 |
| `s68_b_1_B/3`, `s68_b_1_C/3`, `s68_b_1_D/3` | 2 |
| `s151_a/3` | 2 |
| `s151_d_1/1` | 1 |
| `s151_d_2/4` | 3 |
| `s151_d_3_A/7` | 6 |
| `s151_d_3_B/5`, `s151_d_5/2` | 1 |
| `s3301/6` | 6 |
| `tax/3` | 3 |

Basis: `human/DECISIONS.md:913` and the corresponding signature sections of
`docs/consult/evidence/q024-question-bindings/BINDINGS.md`. The three examples
in Q-024 therefore require positions 2, 3, and 3 respectively, without a new
owner choice. Original spot-checks: `human/sara/sara/cases/s1_a_1_i_pos.pl:5,20`,
`s63_c_1_pos.pl:5,20`, and `tax_case_1.pl:33`. `tax/3` remains the H6.1/H6.2
first-solution exception; the annotation never authorizes enumerating it.

Three consequences show why this is not a complete table derived from case
letters alone. Source paths below are relative to `human/sara/sara/`:

| Signature | Consequence under G2 | Evidence |
| --- | --- | --- |
| `s63_d/4` | Positions 2 **and 3** are outputs, although both case modes are `bfbb`; this states required positions, not an exhaustive call-site certificate | `cases/s63_d_pos.pl:20`; the fresh `Total_deduction` in `statutes/prolog/section63.pl:17` |
| `s3306_b/8` | All eight positions are outputs under G2, including in the identifier-bound negative case | H6.5's two modes make every position except 2 free somewhere; position 2 is also free along `section3306.pl:31` into `:18`, where the enclosing `findall` introduces `Remuneration` |
| `s3306_c_B/4` | All four positions are outputs, including positions bound in either listed mode | H6.5 `fbbf` and `bfff`, `human/DECISIONS.md:1048` |

In particular, do not select `[1,8]` merely from `s3306_b/8`'s negative-case
mode or omit bound identifiers on the ground that the question is relational.
For `s152_d_2_D/4` and `s152_d_2_G/4`, positions 3 and 4 are outputs already
forced by `bbff`. For `s68_b/3`, position 2 is forced by `bfb`; the question's
reduction amount occurs in a **later conjunct**, not another argument of
`s68_b/3`. Do not invent an output slot for it or change the root target.

**I cannot certify the remaining exact projections from the submitted evidence.**
`extract.py` inventories the first case call and its question, not statute
call-site instantiation/dataflow. An unfinished G2 derivation is not itself a
missing owner decision. Integration must retain, for each proposed row, the
original call-site/mode basis and any H6.5 bound-answer basis, including why
positions excluded from the tuple are always bound. Send any genuinely
underdetermined row with its specific source sites back for review; do not
fill it by guesswork. This sampled review supplies constraints and the named
classes above, **not acceptance of a complete 135-signature declaration**.

### 3. Existing exceptions and a separate H6.3 finding

Use route 3: derive the shared declaration from DECISIONS and original-source
call-site evidence. Each lane independently conforms to that declaration and
reports discrepancies against the contract. Do not import the harness's
exception list, even through Dev or a new harness session, as shared semantic
authority. HANDOFF's “move” is not an isolation waiver. A counterexample can
return as a contract question without relaying either implementation.

There is an owner-text inconsistency relevant to the extra-conjunct question:
H6.3 (`human/DECISIONS.md:895`) names only the two `s152_d_2_D` cases, but H6.5
also flags `s152_d_2_G/4`, `s68_b/3`, and `s3306_c_10_A/4`. These original
directives confirm additional conjunctions:

- `human/sara/sara/cases/s152_d_2_G_pos.pl:19`;
- `human/sara/sara/cases/s68_a_1_pos.pl:17` and `s68_a_1_neg.pl:17`;
- `human/sara/sara/cases/s3306_c_10_A_i_pos.pl:37`.

This is a **contract clarification for Dev**, not evidence that either lane
has implemented it incorrectly. Dropping the trailing goals would not preserve
the original tests. Proposed minimal replacement for H6.3's “The two cases…”
sentence, recorded as P-H6-CONJ in the decision log, not installed:

> For every original case directive with additional conjuncts, evaluate the
> additional conjuncts on each matching solution of the queried target,
> preserving the original variable sharing, literal order, bindings and
> negation scope. The positive test succeeds iff a solution satisfies the
> entire conjunction; a negated conjunction succeeds iff none does. This
> includes `s152_d_2_D_pos/neg`, `s152_d_2_G_pos`, `s68_a_1_pos/neg`, and
> `s3306_c_10_A_i_pos`. Additional conjuncts do not change the target's
> output-position tuple or the H6.1/H6.2 observation policy.

Dev must approve/amend and install this clarification; no approval is inferred
from the wake or Q. A projection declaration alone cannot settle this scoring
boundary.

## Rationale

The authority is G1/G2 and H6.1–H6.5, not a lane's existing behavior or a green
schema check. WIRE's field-authority table (`Interface/WIRE.md:40`) defers these
choices to those decisions. I reviewed retained question evidence and bounded
original-source samples; I did not execute the extractor, run a corpus audit,
inspect either implementation lane, or interact with the independent meter.

## Contract change

Yes — **PROPOSED only**, P-H6-CONJ, for H6.3's extra-conjunct scope. No new
projection policy is proposed or installed; the declaration location is
builder-owned and the output requirements above follow G2/H6.5.

## Escalate to Dev

Yes — request acceptance/amendment and owner installation of P-H6-CONJ. The
builder must record the affected integration/scoring halt in `STATE.md` and
report it to Dev. Do not finalize the shared declaration/producer release as
approved by A-024: its full G2 derivation is still unreviewed. Read-only
contract/call-site analysis can continue; no other lane or checkpoint is
authorized by this answer. Any out-of-scope delivery write stops the lane.

## Self-review

Assumptions: Prolog bindings are traced at literal entry under G1/G2, not inferred from variable spelling; conjunction and NAF keep their original scope, with no runtime revalidation claimed; three failed edit cycles on one check means stop and report, no fourth attempt or weakened check (no implementation edit cycles here).
