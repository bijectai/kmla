# Q-020 — H4 bound-call evidence and a narrowly scoped draft

**Not sent.** The permission reviewer rejected the consult invocation before
the process started. Explicit owner approval to send this repository excerpt
to Fable/Claude and permit its consult-answer write is required; no retry or
bypass was attempted. No A-020 exists and no review/acceptance is inferred.

## Phase

1, Checkpoint 1 unpassed. Snapshot committed as 6e3cdc9; human unchanged.

## Lane

serializer (design review only; no implementation authorized).

## Question

Dev refused an amendment without the exact failing goal/bindings, correctly
pointing out that H4.2 already binds the first event. Dev permits an amendment
proposal only if that bound call still raises. The fresh pinned execution and
clause-call diagnostic now establish precisely that. Review the narrow draft
below against existing H4.2/H4.3/H2/A3. Is it a sufficiently explicit candidate
for Dev to approve for implementation and verification, WITHOUT claiming it is
already lossless? If not, give the specific missing choice or counterexample;
do not substitute a general unsupported completeness claim. Do not install it.

## What was tried

Original call, unchanged:
`findall(Purpose,purpose_(payment_2015_1,Purpose),Purposes)`.
It raises `error(instantiation_error,context(system:split_string/4,_))`.
Then the diagnostic retrieves the actual original clause and exposes calls in
the same conjunction order (no body rewriting). Immediately before failure:

```prolog
split_string(Service_event,"_","",[Xs,Ys,Zs])
% Payment_event = payment_2015_1 (atom, ground)
% Xp="payment", Yp="2015", Zp="1"
% Service_event, Xs, Ys, Zs are all free.
```

Exit 2, no timeout; final stderr verbatim:
`ERROR: split_string/4: Arguments are not sufficiently instantiated`.
This is the SECOND split, not an unbound first event argument. The diagnostic
also retains the original goal's failure independently of its call tracer.
The first split accepts the bound atom on the pinned interpreter.

Full runtime identity matches RUNTIME.json:
`744cbb885225c77569618a35c4c856f5c4a6c6e4fb6ecb37f7476eb46bbf5c92`.
Immutable image:
`sha256:8e53d3a0003635786ccdeafc0048b3344b621815fe4881fd4512914a95606ec7`.

A-017 is corrected by A-019: 157 payment and 157 service solutions, 157
successful pairs on that declared product. Neither global completeness nor
H4.3 preservation was established. A-019's place-purpose call-site observation
does NOT measure a failure of a tax_case_33-specific candidate. Do not treat
it as such. H4.3 already fixes preservation; no stronger criterion is requested.

## Files involved

- human/DECISIONS.md H2/H4.2/H4.3/A3 (read-only).
- human/sara/sara/cases/tax_case_33.pl:29–31 (read-only), SHA-256
  `5c04303da3dfdf96c412994cb0aaa3ce01c5d248d9de01ed4726456ea523ffa0`.
- docs/consult/evidence/h4_bound_goal_bindings.pl (diagnostic, not a grounder).
- docs/consult/evidence/h4-bound-goal-2026-09-23/commands/005.* and identity.json.
- docs/consult/evidence/q019-counts/001.* (numeric diagnostic only).
- A-017/A-019 and proposed P-GROUND2; do not edit old answers or log bodies.

## Proposed answer (DRAFT, not selected or installed)

Possible narrow appended H4.2 exception:

> For the single `purpose_/2` rule at lines 29–31 of the pinned original
> `tax_case_33.pl`, step (ii) binds both inputs. Keep the first-position event
> traversal from step (ii). For each such event, traverse the ground `service_/1`
> terms obtained in step (i), once per distinct tagged term in first-occurrence
> order, and execute the original `purpose_(Event,Service)` with both arguments
> bound. Append every successful proof of that call in interpreter order; never
> deduplicate its proof results. This second-input domain traversal is scoped
> to that exact original rule, not a replacement for other purpose clauses.
> Bodyless wildcard facts retain H2/A3 patterns and shared variable identity;
> no wildcard is expanded by this exception. All other H4.2 steps and H4.3(a,b)
> stay unchanged. A raise, nontermination or failed H4.3 check remains a finding,
> never an empty result, skipped original, broader candidate domain or source
> repair. Report both H4.3 checks for every one of the 376 originals before
> claiming round-trip completion.

This is explicitly an input-domain and ordering PROPOSAL to the owner. It
does not promise the original predicate's infinite ground extension, does not
claim existing H4.3 success, and grants no implementation authority until Dev
approves. First-occurrence dedup applies only to this proposed candidate domain,
not to source facts or successful Prolog proofs. If a more general procedure
is required by another finding, it needs its own evidence/approval; no implicit
fallback is allowed. Existing 157-pair evidence is not a cost measurement for
this proposed full event-by-service traversal.

## Review boundary

Answer only; no source, grounder, Interface, protected artifact or approved
contract edits. Preserve prior A files. If recording a proposal correction,
append it as PROPOSED with the current evidence and no acceptance claim. Do not
turn the open proof obligations in the separate oracle lane into a blanket
hold: Dev has authorized its continued §7703 translation and one proof-only
round. End with a self-review listing assumptions about Prolog semantics and
the circuit breaker: three failed edit cycles on one test means stop and report.
