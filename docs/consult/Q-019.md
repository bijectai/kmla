# Q-019 — correct A-017's count and completeness claims before Dev decides

## Phase / lane

Phase 1, serializer design review only. Grounding remains halted. This question
does not authorize an alternative grounder or any semantic/Interface change.

## Question

Correct A-017/P-GROUND2's factual scope by appending a correction, never editing
A-017. Restate the minimal owner choice without claiming that an event-universe
procedure is complete for the entire purpose_/2 relation. Distinguish H4.3's
actual preservation criterion (re-grounded lists and the original case's queried
goals) from global equivalence over all bound Prolog terms. Do not select a remedy.
Keep the established H4.2 mode failure and owner escalation, if still justified.

## What was tried

A-017 claimed the true ground extension of tax_case_33's purpose clause is 87
pairs, called event-universe enumeration complete for that case, and used 157²
as if it represented the full event universe. Source inspection shows a second
2016 family in addition to 2015, and the purpose rule itself does not require
payment_/service_ membership.

A read-only numeric/domain diagnostic on the same immutable pinned image loaded
the EXACT retained reader-approved original clauses. It counted declared
payment/service solutions and successes of purpose_ on their finite product,
then checked just one additional fully ground pair absent from BOTH unary lists.
It emits only this diagnostic, no Household or producer record; this is not an
installed grounding procedure or approved candidate ordering.

Verbatim stdout (legacy JSON library renders our truth-status atoms as strings):

```json
{
  "declared_payment_solutions":157,
  "declared_service_solutions":157,
  "declared_pair_solutions":157,
  "outside_pair_succeeds":"true",
  "outside_payment_declared":"false",
  "outside_service_declared":"false"
}
```

The outside pair is purpose_(payment_2099_999, workforalice_2099_999).
Process exit 0, no stderr, no timeout. Neither named event occurs in its
respective enumerated unary solutions. The source clause admits analogous
matching names beyond the declared events, so a finite enumeration is not by
itself the complete ground relation. The number 157 is the payment count AND
service count, not the count of all unary event markers (which also include
marriage and joint return in this case). No claim about a complete grounding
universe or all 376 H4 round trips follows from this diagnostic.

## Files involved

- A-017 and proposed P-GROUND2 in docs/DECISION_LOG.md.
- human/DECISIONS.md H4.2/H4.3, H1/H2/G1/G2, read-only.
- human/sara/sara/cases/tax_case_33.pl:13–14,26–31,40–41,51–52.
- docs/consult/evidence/q019_grounding_counts.pl.
- docs/consult/evidence/q019-counts/001.command.json, 001.stdout, 001.stderr.
- Input reader material: docs/phase1/harness-evidence-2026-09-23/h4-probe-docker/requests/001.pl.

## Proposed answer, if any

No remedy selected. The 87 figure covers only the declared 2015 family, not the
full finite declared family pair count and not the global relation. A proposed
amendment must say what preservation domain it promises; completeness cannot be
asserted from unary enumeration. Preserve wildcard identity under the signed
H2/A3 representation unless Dev explicitly amends it. Do not present an
unverified cost estimate as a measured result.

Answer in A-019 and append a clearly marked correction to the PROPOSED log
entry; do not rewrite its history or install a design. No protected writes.
End with self-review listing Prolog assumptions and the circuit breaker: three
failed edit cycles on one test means stop and report.
