# Q-015 — bind V3 to supplied Workdays before isolated Phase 1 dispatch

Phase: 1 entry. Lane: infra/shared Interface (neither implementation lane).

## Question

Dev now confirms Checkpoint 0 at `0a2a65a`, installs/re-pins approved A, and
authorizes Phase 1 after read-only runner/CI enforcement and closing V3's
query-/stipulation-supplied Workday gap. R5 work proceeds with proofs open.
This supersedes the earlier no-oracle-work scope; no new semantic choice is
requested. Is the concrete boundary integration below faithful and sufficient
as implementation scaffolding (not source-mode/termination certification)?

## What was tried / files involved

`Interface/Household.lean` V3 checks only fact days and the outer year.
`Interface/QueryTime.lean` checks an actual year/Workday and carries a dependent
certificate, but is not connected to household admission. `human/DECISIONS.md`
H5 V3, H4.1, D1/D2, amended R5/R8 require that connection. A-013/A-014 leave
operational-mode completeness as a proof obligation. No protected source is
edited. Do not inspect the owner's parity meter or either implementation lane.

## Proposed handling

1. Put an exact D1 canonical ISO decoder in Interface: parse civil date,
   round-trip through `Day.toISO`, check V3 bounds. For stipulated Workday
   `s3306_c/5` argument 4, validate each concrete date (both `atom` and `str`
   textual tags, retaining the original tag and value); a wildcard introduces
   no date and is preserved, with its eventual bound call value checked.
   Include this check in household V3, hence both Valid and ValidStip. Missing
   arguments remain a shape error, not a wildcard/default. No blanket ban on
   all apparent stipulation-year literals: the inert `s68_b(...,250000)`
   finding stays as written, per A-014.
2. Shared typed query arguments distinguish `Term`/pattern, `Day`, and `Year`;
   a query's V3 check traverses the actual argument tuple, not a separately
   supplied list of allegedly used dates. A query-admission certificate is
   indexed by that exact tuple and combines the existing Valid/ValidStip
   premise with these argument checks. Every producer and oracle entry wrapper
   must consume it, while actual R8 calls additionally consume the existing
   `CoveredR5Time (.workday actual)` certificate. No V8 recheck at Workday's
   derived year is introduced. The codec/target schemas in Interface must fix
   roles so producers cannot mislabel a Workday as a generic Term.
3. Schema completeness, all internal call-site coverage, wildcard binding,
   E2 exclusion, exact V10 decider and fuel adequacy remain explicit proof
   obligations, not prerequisites to *starting* their authorized implementation.
   Decode/admission/coverage failure is a diagnostic and halts the affected
   reference run, never a normal oracle answer or a filtered original.

Please identify any needed correction, especially a supplied Workday signature
missed in H4.1, tag handling, or whether a different connection to V3 is needed.
Write A-015 only (no decision-log edit needed unless an actual contract change
is identified). Do not implement a lane or change approved semantics.

End with a self-review listing assumptions about Prolog semantics and the
circuit breaker: three failed edit cycles on one test means stop and report.
