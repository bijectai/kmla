# Q-021 — measured country-fact loss: implementation error or missing choice?

## Phase

Phase 1; Checkpoint 1 unpassed. Dev authorized the exact Q-020 candidate's
implementation and verification after endorsement, not protected installation.
A-020 endorsed it. The three requested prior commits were pushed first.

## Lane

Harness/serializer. The affected generic grounding path is halted on the
concrete mismatch below, already reported to Dev. No revised traversal has
been applied. This consult does not authorize any protected change.

## Question

Does preserving the original ground `country_/2` facts follow directly from
H1/H3 (so applying an event-position restriction to a place is a builder bug),
or is a new owner choice needed to supplement H4.2? Please separate those
possibilities with exact signed text. If a correction is already specified,
name its narrow scope and the required regression; do not silently choose a
new domain or bypass ordinary step (ii) for all bodyless event facts.

The two-input `tax_case_33` candidate itself passes its measured H4.3(a,b)
checks. This finding is in the generic event-domain traversal, not evidence
that a different service domain is needed for the scoped exception.

## What was tried and measured

The current helper enumerates each supplied unary clause's solutions, retains
unary provenance, then visits supplied binary clauses with position 1 bound
to each unary event. It handles the two approved wildcard facts separately and
the exact tax_case_33 exception separately. It uses source-clause provenance
to retain H1 order. It does **not** currently distinguish a `country_/2` place
position from an event position. No deduplication or bodyless bypass was added
in response to this finding.

Original `s3306_c_A_pos.pl:15` supplies:

```prolog
country_("baltimore, maryland, usa","usa").
```

File SHA-256:
`4f788e912133535b3433f32fa53d489a9d5933f5f674b485b141ce92d784012b`.
That literal is omitted by this grounder. Re-grounding its emitted Household
still yields the same lists (H4.3(a) passes), but the actual H6.5 `bff` query
`s3306_c_A(alice_employer,Employer,Employee)` changes. Raw stdout, verbatim:

Original (`country-finding/commands/007.stdout`):

```text
[ [ {"a":"alice"},  {"a":"bob"} ] ]
```

Serialized (`country-finding/commands/008.stdout`):

```text
[]
```

Both commands exit 0, no timeout; the mismatch is not a runtime exception or
failed search interpreted as refutation. This compares the full two-position
answer, not the truth of a ground expected-answer directive. Both H4.3 checks
are required, and the second fails.

The complete runtime identity matches
`744cbb885225c77569618a35c4c856f5c4a6c6e4fb6ecb37f7476eb46bbf5c92`, image
`sha256:8e53d3a0003635786ccdeafc0048b3344b621815fe4881fd4512914a95606ec7`,
with `/human` and `/corpus` mounted read-only.

## Repository excerpts and files involved

- human/DECISIONS.md H1: source-order facts, duplicates kept; no keyed,
  deduplicated, sorted or defaulted representation.
- H3 explicitly gives `country_/2` the roles **(place, country string)**,
  four statute reads and fourteen supplied case clauses. It also requires
  predicates with zero statute reads to round-trip.
- H4.2(ii) says "every binary event predicate with the event position
  bound to each event, collecting all solutions with multiplicity".
- H4.3 requires both ordered-list re-grounding and queried observations.
- H6.5 explicitly lists `s3306_c_A/3` in `bff` mode.
- human/sara/sara/cases/s3306_c_A_pos.pl (read-only).
- harness/grounding.pl `binary_solution/6`, harness/grounding.py,
  harness/grounding_findings.pl and scripts/check_grounding_findings.py.
- docs/phase1/h4-candidate-evidence-2026-09-23/country-finding/finding.json
  plus complete requests, runtime identity and commands there.
- docs/contracts/H4_BOUND_PURPOSE_DRAFT.md and immutable A-020.

## Proposed answer, not an implemented remedy

Likely a builder classification bug: H3 says the first position is a place,
so applying H4.2's event-position restriction there is unsupported. H1/H3
appear already to require retaining these supplied ground clauses with their
source order, tags and multiplicity. Confirm or reject that reasoning; do not
promote it to an owner acceptance. In particular, preserving these non-event
facts would not authorize bypassing step (ii) for every bodyless event fact,
deduplicating the outer event traversal, widening the tax_case_33 domain, or
changing comparisons to make the mismatch disappear.

If you find an actual missing choice or design conflict, escalate it to Dev
before any implementation of a remedy. If it is an implementation violation
of existing signed requirements, say precisely why and what remains open.

## Review boundary and self-review request

Write A-021 and, only if useful, append a clearly labelled review entry to
docs/DECISION_LOG.md. Do not edit old A files, the standing prompt, protected
artifacts, implementation or tests. No new acceptance or installation.
Other non-tax output projections remain builder implementation work wherever
H6 determines them; their absence is not itself an owner-choice blocker.

End with your assumptions about Prolog semantics and the circuit breaker:
three failed edit cycles on one test means stop and report.
