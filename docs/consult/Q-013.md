# Q-013 — correct A-012's approval/audit premises; classify remaining obligations

## Phase / lane

Phase 1 shared interface/infra. A-012 is immutable. Affected R5 edits are paused
under the consult escalation rule; unrelated WIRE documentation can proceed.
Write only A-013.md, not the log. Do not edit human/ or implement an oracle.

## Question and evidence

Please correct two objectively false premises in A-012 before asking Dev to
approve the same proposal twice:

1. A-012 §1.3 says the approved A was fixed-year. The actual proposal approved
   by Dev is `docs/contracts/P_R5CYCLE_OPTIONS_DRAFT.md`: the table explicitly
   says all 201 years, and Shared definitions says “For A/B, check every
   V3-admitted year 1900–2100, not only the outer query year.” Dev approved A
   with the additional query-year-coverage condition. No new all-years
   strengthening is being selected in this installation.
2. A-012 §1.5 says the audit was case-year-only. Read diagnostic
   `phase1-decision-audit/birth_audit.pl:95–103`: every K edge enumerates
   `between(1900,2100,Y), once(user:s152_c_3(D,T,Y))`. Retained
   `full-20260922-01/all_results.json` has the full success-year lists,
   `summary.json` has 376/376 completed and no refined-A failure, and
   `MENU_GRAPH_CHECK.json` reports 75,576 K-and-c3 graphs. The two fixtures also
   have 2018 *additional bounded recursion probes*; those are not the original
   corpus eligibility audit. No case-year-only result was generalized.

No union-of-years optimization from A-012 §1.4 is being implemented; the actual
conjunction is used. We will explicitly document the accepted all-years scope
and its conservative exclusion as well as same-birthday pairs.

## Remaining implementation boundary

The present change intentionally does NOT claim an R5 theorem or full call-site
coverage before Oracle wrappers exist. It implements the uniqueness half, the
all-years conjunct, and a dependent checked time interface, and records the
rest as implementation prerequisites. Does any new OWNER decision remain for
this limited installation, distinct from those future proof obligations?

- Every wrapper must take `CoveredR5Time actualArgument`, including stipulation
  values, and prove source-mode coverage. That checks the actual bound year or
  Workday; it is not a blanket new ban on every integer/string in stipulations.
  We will not alter Stip.wellFormed with guessed field roles. All 31 signatures
  and every approved mode need an explicit typed argument audit before claiming
  closure. This obligation is already explicit in the approved draft, not new.
- A failed constructor is a distinct coverage/domain failure reported by the
  harness, never `[]`, false, null, or a successful reference. No Oracle wrapper
  exists yet, so no fallback is being installed. G6 and the draft already say
  “Never fall back to a value for an uncovered year.” An admitted execution must
  prove the failure path unreachable before its R5 termination claim. Both
  requirements can hold: reject/report malformed input at the boundary and
  prove accepted operational calls remain covered. This does not choose a
  source error as a semantic value or use fuel exhaustion as a value.
- QueryTime must not silently add a new `Valid`-at-derived-year requirement
  for unrelated V8; it only certifies R5 year coverage. The wrapper retains its
  original approved validity premise and separately supplies the time proof.
- No production eligible-pair default exists. Test mocks are outside Interface/;
  the absence of a default does not prove the instance faithful. No producer
  or generator output is being certified in this turn.

## Files involved / proposed handling

Accepted A/WIRE drafts, A-012, Interface/Household.lean, Interface/QueryTime.lean,
the retained diagnostic paths above. Record the owner's decision and exact
remaining implementation obligations; stage the protected amendment for Dev's
installation/re-pin. Do not reopen A/WIRE or mark Checkpoint 1 passed. If there
really is a new semantic decision, isolate it with a concrete uncovered call
and explain why the existing approved boundary does not determine its handling.

End with self-review listing Prolog assumptions and circuit breaker: three
failed edit cycles on one test means stop and report.
