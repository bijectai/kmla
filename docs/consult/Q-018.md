# Q-018 — §7703 slice test-proof circuit breaker

## Phase

Phase 1.2, first isolated §7703 context; Checkpoint 1 is unsigned. The earlier
preflight query-admission proof stop was resolved on owner-authorized
continuation. This is a NEW stop on three different tests, not permission to
reset the old counter or continue editing through a breaker.

## Lane

oracle. The lane has stopped and its context is closed. Review only: no further
proof/implementation edits are authorized by this question. The coordinating
task will report the required resumption direction to Dev.

## Question

Give a bounded diagnosis and kernel-checked proof plan for the three EXACT
statements below on pinned Lean 4.33.1. Classify whether a shared implementation
defect is actually established. Do not weaken/remove tests, use native_decide,
axioms or mock providers, change a mode/domain/observation, or edit Interface.
Do not rely on all well-founded definitions being irreducible: A-016's
renderer-failure prediction did not reproduce in the prior diagnostic.

## What was tried

`Oracle/S7703.lean` compiles (six independent clauses and a parameterized b1
body, no recursive root/provider). `Oracle/Tests/S7703.lean` has 53 theorem
declarations: 50 elaborate without errors, three fail. Whole test command exits
1; failed-proof recovery `sorryAx` is NOT an accepted proof/artifact.

1. `b1_exact_duration_passes_same_time_and_keeps_dependency_multiplicity`:
   first simp, then unfolding List bind/pure, then additionally tag-sensitive
   BEq. Final residual goal is a concrete `min`/Int guard around flatMap over
   an arbitrary continuation. The original certificate and multiplicity must
   remain unchanged.
2. `a1_null_only_at_observation` and
   `b3_deduplication_only_at_observation`: decide, then decide +kernel, then
   rewriting raw results and `String.foldl_eq_foldl_toList` before decide.
   Final failures still say the Decidable instance did not reduce to isTrue or
   isFalse. The observation includes escaping, intercalate, mergeSort and foldr.

All three reached three failed edits. No fourth attempt, assertion change or
implementation alteration followed. The coordinating task reproduced the
unchanged command read-only. No parity mismatch is established by failed proof
reduction. The separate H4/Q-017 grounding hold remains unrelated.

Final exact b1 residual goal:

```text
checked : CoveredR5Time (R5TimeSource.year y)
dependency : Term → Term → (year : Year) → CoveredR5Time (R5TimeSource.year year) → List Unit
⊢ List.flatMap
      (fun __x =>
        List.flatMap (fun __x => [(Term.atom "home", Term.atom "bob")])
          (dependency (Term.atom "bob") (Term.atom "alice") 2015 checked))
      (if 364 ≤ 2 * (min 16618 16800 - 16436) then [()] else []) =
    List.flatMap (fun __x => [(Term.atom "home", Term.atom "bob")])
      (dependency (Term.atom "bob") (Term.atom "alice") 2015 checked)
```

Exact statements/tactics and reproducible expanded diagnostics are retained in
the unmodified test file and progress report. The existing reductions diagnostic
prints available String.foldl_eq_foldl_toList and derived instBEqTerm.beq;
raw `#reduce escapeJson "b"` hit the default recursion limit. Resource limits
were not disabled.

## Files involved

- Oracle/S7703.lean (this lane only, not harness code).
- Oracle/Tests/S7703.lean, particularly the three named statements.
- Oracle/Tests/S7703Reduction.lean.
- Interface/Household.lean observation definitions.
- docs/phase1/ORACLE_7703_PROGRESS_2026-09-23.md (commands, failed goals, hashes).
- human/DECISIONS.md X1/G1/H6, read-only. Never inspect the meter or exploits.

Build outputs already exist at /private/tmp/kmla-oracle7703.IqVF77. Reproduction:
`LEAN_PATH=/private/tmp/kmla-oracle7703.IqVF77 lean +leanprover/lean4:v4.33.1 Oracle/Tests/S7703.lean`.

## Proposed answer, if any

No proof fix selected. The arithmetic goal appears concrete and decidable, but
the arbitrary continuation must not be replaced by a test mock. The shared
observer failures require isolation, not a guess that semantics are wrong.
Return a review in A-018 only; do not perform the next edit cycle yourself.

End with self-review listing assumptions about Prolog semantics and the circuit
breaker: three failed edit cycles on one test means stop and report.
