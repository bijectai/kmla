# Phase 1 re-entry — 2026-09-22

Owner-authorized parent: `0a2a65ac1313c180bea39d137e6665c01b8e833a`.
Checkpoint 0 is signed off. A and WIRE are installed/approved; Checkpoint 1 is
not signed off. No builder changes under human/ are authorized.

**Final status: preflight incomplete; no isolated lane dispatch.** The new
concrete tuple-admission proof reached the three-failed-edit circuit breaker.
`scripts/check_query_time.sh` is red. The implementation below remains work in
progress; retained diagnostics are in
`QUERY_ADMISSION_CIRCUIT_BREAKER_2026-09-22.txt`. No test was weakened/removed.

## Protected identity

`python3 -B scripts/human_manifest.py verify` reports:

```text
verified: every file under human/ is listed and matches its digest
```

| Artifact | SHA-256 |
| --- | --- |
| human/DECISIONS.md | `12d534e2ea589f97dfd27d93ddebb88e37cbc259af66ebe7e1b54f03d7f8686a` |
| human/HASHES.txt | `5ff23beb28eacdf18d4428395e1d96e4fc8123a8f51ae29abc7943bf0f6062b9` |
| human/parity/check.py (hash only; source never read) | `c5cc94a60437d393b302a87f6c6fd40d1a1758e610c121f65662c014ff3c92e5` |

No manifest regeneration or protected installation was performed by the builder.

## Shared admission boundary

The previous checked-year type alone did not connect V3 to supplied values.
The integration adds canonical D1 decoding and signature-derived stipulated
Day checks directly to Household.v3 (both validity predicates). Query admission
is indexed by the actual predicate/argument tuple, not a producer's separate
list of alleged times. Unknown signatures and wrong arities fail closed.
All concrete Day/Year arguments are checked against the shared role schema;
Day-list outputs are not confused with one Day because of a variable name.

Wildcard positions remain wildcard positions with the same identity. They
introduce no concrete time and are not evidence of bounded operational calls.
When bound at a call site, the actual value must be checked. Tags stay intact:
atom and string dates have the same arithmetic but different unification.
Malformed or overflowing ISO spellings and integers in a Day slot fail D2/V3;
they are not normalized or coerced. There is no V8-at-derived-year addition.

The two inert `s68_b(alice,2015,250000)` heads stay unchanged and reported.
No blanket rejection of stipulated year literals is introduced. Admission
checks actual query/call years instead. An error never becomes a reference
value or permission to drop an original. All 376 originals remain accounted for.

Q-015/A-015 reviews the mechanism and requires a schema-wide date inventory and
canonical-form census, not a Workday-name scan. The signature declaration and
census are retained separately under Interface and docs/phase1.

Final census: 175 shared time signatures, 135 queried and 31 stipulated;
all 376 original audit records checked. All 162 concrete stipulated Day
occurrences are canonical and bounded; four Day wildcards stay unchanged.
Zero originals are excluded by this measured Day check. It does not discharge
the failed kernel regression or general operational coverage.

## Local verification

- Strict Checkpoint 0: 17 passed, 0 failed, 0 outstanding; two later artifact
  groups still reported. This confirms the entry checks, not Phase 1 preflight.
- Runtime regressions: 29 pass. Runner/CI boundary regressions: 12 pass.
  Actual Docker inspection in RUNNER_BOUNDARY_2026-09-22.md verifies /human
  and /corpus read-only. Hosted Actions have not run the new workflows.
- Household guards: 115 pass at the recorded integration check; the generated
  schema consistency check passes for all 175 signatures.
- Observation tests: 3 pass; parity invocation tests: 4 pass; verifier tests: 10 pass.
- Query-time suite: exit 1, exact diagnostics retained, not reported as passing.
- Final protected manifest and git diff whitespace checks pass. human/ and
  docs/PLAN.md have no changes. Work remains uncommitted; no push/PR/merge.

## Generator boundary

gen/AGENTS.md and gen/README.md explicitly treat V10 as a pre-execution validity
boundary: never mint multibirth persons, multi-date births or same-birthday
eligible pairs. Preserve unrelated same birthdays and identical repeated facts.
Do not inspect engine answers to decide admission, drop frozen records, replace
failures or shrink denominators. Record invalid construction attempts separately.
No generator is implemented or Phase 2 authorized by this confirmation.

## Completion limits

The shared checks are not a production OracleGuards instance, a complete target
codec, a source-mode theorem, R5 termination, semantic round-trip identity or
Lean/Prolog parity. Exact K/c3 eligibility (including wildcard patterns), V7/V8,
descendant correspondence, operational coverage, finite participant universe,
measure/phase and fuel adequacy remain work for the authorized oracle lane.
R5's original child-only argument and the withdrawn 2·persons+2 bound stay
withdrawn. Exhaustion must halt/report and never stand in as a reference value.

The two implementation contexts share only the semantic decisions and Interface
(and original read-only source); neither may inspect the other's implementation.
Each §7703, 3306, 3301, 2, 63, 68, 151, 152, 1 translation uses a fresh context
in that order. All mismatches are retained verbatim; no semantic repair/filter.

## Self-review

Assumptions: D1/D2 authorize canonical civil-day decoding; G4 requires preserving
the original textual tag; a wildcard is not a supplied concrete date; a head
literal need not become an operational year because unification may fail.
These come from the signed decisions and retained source evidence, not from
another lane's implementation. No claim about Prolog exception behavior or
complete call coverage follows from these structural checks. Three failed edit
cycles on one test means stop and report; that threshold was reached on the
new concrete admission proof, and affected development/dispatch stopped.
