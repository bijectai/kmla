# Native amd64 baseline: run 35649969714, attempt 1

Verified on 2026-09-21. This is a Prolog directive baseline, not Lean parity
or Checkpoint 0 sign-off. No owner meter was present or inspected.

## Provenance and retention

- Run: https://github.com/bijectai/kmla/actions/runs/35649969714
- Commit: `b265977f317dd7d2b8f1cfa3dbe367e67f0d5000`
- Job: `runtime-baseline`, native Linux X64, completed successfully.
- GitHub artifact: `runtime-baseline-35649969714-1`, ID `10661946757`.
- GitHub reports the uploaded ZIP SHA-256 as
  `8c20f8771178268b0dda72372cbec51d3019c8b4a5e248776e0f33c02d3e92ac`
  (378144 bytes; expires 2026-12-20). This is API-reported, not a locally
  recomputed ZIP digest: `gh run download` extracts the download.
- Retained bundle: `native-35649969714-1.tar.gz`, SHA-256
  `7cf618d3a1ae83cd410a643f0c7954b607116119829cd8934faab996562cde46`
  (136097 bytes). This is a local repack, not the original GitHub ZIP.
  All **1546 downloaded files** were verified byte-for-byte against the
  repacked members, with no missing, duplicate, symlink or unsafe member paths.

The bundle contains the unmodified `RUNTIME.json`, `BASELINE.json` and both
diagnostic directories, including build/probe command output, run context,
source hashes, case IDs, and all per-case status/stdout/stderr files. Absolute
paths inside these records refer to the original CI runner and are not rewritten.

## Measured result and verification

- **376/376 clean directive outcomes**, including `s3306_c_2_neg` and
  `s3306_c_2_pos`, which execute no test: **374 nonvacuous cases**, not 376
  exercised assertions. All 376 distinct IDs match the installed corpus.
- All 376 raw statuses agree with the reported zero exits and durations;
  all 752 raw stdout/stderr files are empty and match their recorded hashes.
- The runtime record equals both the baseline's embedded runtime and the
  separate pre-sweep measurement. Recomputing identity over the whole record
  except `image.local_image_id` gives
  `d053110214bd538340e238dd6251b1b5692a132617eb0aa318298daf61abfe31`.
- `RUNTIME.json` file SHA-256:
  `b61a74746f93cb27cdf8038a3a41d7a2a821bb3757aa31e4f50d330b0f64b914`.
- `BASELINE.json` file SHA-256:
  `dd0f2527c6d0ba78132679040f3fd5a1b9d18b976f299db939f7469f71c617fd`.
- SWI-Prolog `7.2.3`, `PLARCH=amd64`, Debian `7.2.3+dfsg-6`,
  `TZ=America/New_York`, 102 installed packages. The actual Prolog process
  executable, ELF architecture and Docker daemon establish native amd64;
  this is not an inference solely from the runner label.
- All 388 recorded case/statute source hashes, the JHU tarball digest,
  the five recorded harness file hashes and Dockerfile hash match this checkout.
  Source archive: `e4f1b845016fc38b95b702bbbb27a5c9af3eadfa856ba585662b8b2cd71c187c`.
  Dockerfile: `fc26076b088477b1e3c56af553ecbc6f916a69b297114cbd1a76ec734493d448`.
- The retained execution has exit 0, no timeout, a read-only corpus mount,
  and no baseline problems. The CI job also passed all 18 runtime unit tests.

Retrieval commands:

```sh
gh run view 35649969714 --json status,conclusion,jobs,url,displayTitle
gh api repos/bijectai/kmla/actions/runs/35649969714/artifacts
gh run download 35649969714 --name runtime-baseline-35649969714-1 --dir FRESH_DIRECTORY
```

Local validation independently recomputed the JSON identity, file digests,
population and per-case statuses described above, then read every member of
the generated tar archive and compared it with the extracted download.

## Limits and remaining obligations

This native result is tied to its own runtime identity; it does not retrospectively
supply a sweep for the separate local Rosetta runtime record. All earlier runtime
records and controls remain unchanged. Neither corpus success nor hash equality
establishes equivalence on every possible input or independence of an absent meter.

This bundle contains **diagnostics, not a saved Docker image or rebuild closure**.
GHCR publication, offline rebuild materials and the independently deposited image
archive remain incomplete. The Checkpoint 0 meter, covering manifest and explicit
owner sign-off are still required before Phase 1 starts.
